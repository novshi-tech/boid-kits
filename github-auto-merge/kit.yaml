builtin_commands:
  - boid

env:
  BOID_MERGE_POLL_RETRIES: "6"
  BOID_MERGE_POLL_INTERVAL: "10"

scripts:
  - id: auto-merge
    description: "完了タスクの PR を自動マージし、コンフリクト時はタスクを reworking に戻す"
    on: [task_done]
    filter:
      behavior: dev
