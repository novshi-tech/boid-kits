additional_bindings:
  - source: ${HOME}/.volta
  - source: ${HOME}/.codex
    mode: rw
  - source: ${HOME}/.local/share/boid/skills/boid-sandbox
    mode: rw

hooks:
  - id: run-agent
    on: executing
    traits:
      consumes: [instructions]
    requires:
      - codex
      - python3
